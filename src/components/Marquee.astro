<div class="marquee" data-marquee aria-hidden="true">
  <div class="marquee__track" data-marquee-track>
    {
      Array.from({ length: 4 }).map(() => (
        <span class="marquee__item t-display t-display--lg">
          AI that <span class="t-tilt">ships</span>
          <span class="marquee__sep">—</span> NORU<span class="t-tilt">zo</span>
          <span class="marquee__sep">—</span>
        </span>
      ))
    }
  </div>
</div>

<style>
  .marquee {
    overflow: clip;
    padding: 2rem 0;
    background: var(--c-paper);
    color: var(--c-ink);
    border-top: 1px solid var(--c-line);
    border-bottom: 1px solid var(--c-line);
  }

  .marquee__track {
    display: flex;
    width: max-content;
    will-change: transform;
  }

  .marquee__item {
    white-space: nowrap;
    padding-right: 0.6em;
  }

  .marquee__sep {
    color: var(--c-line);
    padding: 0 0.15em;
  }
</style>
