<section class="philosophy section" id="philosophy" data-theme-section="dark">
  <p class="philosophy__label t-label" data-reveal>Philosophy</p>

  <p class="philosophy__manifesto t-display t-display--md" data-split>
    We think AI should be judged like any other hire: by the work it does.
    So we build systems that show up every day, measure them honestly, and
    cut what doesn't perform. Craft over noise. Output over demos.
  </p>
</section>

<style>
  .philosophy {
    min-height: 90svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 3rem;
  }

  .philosophy__label {
    color: var(--c-accent);
  }

  .philosophy__manifesto {
    max-width: min(100%, 980px);
    color: var(--c-paper);
    font-weight: 500;
  }
</style>
