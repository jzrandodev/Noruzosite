<section class="hero" id="top">
  <canvas class="hero__gl" data-gl aria-hidden="true"></canvas>

  <div class="hero__content">
    <h1 class="hero__title t-display t-display--xl" data-hero-title aria-label="Noruzo AI">
      <span class="hero__line" aria-hidden="true">
        <span class="char">N</span><span class="char">O</span><span class="char">R</span><span class="char">U</span><span class="char t-tilt">z</span><span class="char t-tilt">o</span>
      </span>
      <span class="hero__line hero__line--sub" aria-hidden="true">
        <span class="char">A</span><span class="char">I</span><span class="hero__dot char"></span>
      </span>
    </h1>

    <p class="hero__tagline t-body-lg" data-hero-tagline>
      An AI agency for people who measure magic in shipped product.
      We build automation, agents and AI products that earn their keep.
    </p>

    <a class="hero__scroll t-label" href="#services" data-hover>
      Scroll <span class="hero__arrow" aria-hidden="true">↓</span>
    </a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: flex-end;
    padding: calc(var(--nav-h) + 2rem) var(--gutter) clamp(2rem, 6vh, 4rem);
    overflow: clip;
  }

  .hero__gl {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    /* Static fallback painted until (or instead of) WebGL boots */
    background: radial-gradient(
      110% 80% at 72% 18%,
      #ffd9c7 0%,
      var(--c-bg) 62%
    );
  }

  .hero__content {
    position: relative;
    width: 100%;
  }

  .hero__title {
    color: var(--c-ink);
  }

  .hero__line {
    display: block;
    overflow: clip;
  }

  .hero__line--sub {
    display: flex;
    align-items: baseline;
  }

  .char {
    display: inline-block;
    will-change: transform;
  }

  .hero__dot {
    width: 0.16em;
    height: 0.16em;
    border-radius: 50%;
    background: var(--c-accent);
    margin-left: 0.08em;
    align-self: flex-end;
    margin-bottom: 0.09em;
  }

  .hero__tagline {
    max-width: 32rem;
    margin-top: 2rem;
    color: var(--c-ink-soft);
  }

  .hero__scroll {
    position: absolute;
    right: 0;
    bottom: 0.25rem;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--c-ink-soft);
  }

  .hero__arrow {
    display: inline-block;
    animation: bob 1.6s var(--ease-in-out) infinite;
  }

  @keyframes bob {
    50% {
      transform: translateY(4px);
    }
  }

  @media (max-width: 640px) {
    .hero__scroll {
      display: none;
    }
  }
</style>
